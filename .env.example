# Copy to .env (git-ignored) and fill in. Import into the macOS Keychain with:
#   swift run citration openalex-key import-env
OPENALEX_API_KEY=
# Mistral OCR for scanned documents; imported the same way.
MISTRAL_API_KEY=
