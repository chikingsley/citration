# Unused Cross-Platform Contracts Placeholder

Citration’s current clients are native Apple applications and share Swift models through `packages/citration-core-swift`. The canonical remote contract is Zotero API v3 rather than a custom Citration synchronization protocol.

This placeholder is scheduled for removal. It should return only if a real non-Swift client needs generated schemas or fixtures; it must not become a second library object model.
